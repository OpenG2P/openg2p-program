<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Tag Form View -->
    <record id="view_support_tag_form" model="ir.ui.view">
        <field name="name">support.tag.form</field>
        <field name="model">support.tag</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button" />
                        </button>
                    </div>
                    <group>
                        <field name="name" />
                        <field name="color" widget="color_picker" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Tag Tree View -->
    <record id="view_support_tag_tree" model="ir.ui.view">
        <field name="name">support.tag.tree</field>
        <field name="model">support.tag</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
            </tree>
        </field>
    </record>

    <!-- Tag Action Window -->
    <record id="action_support_tag" model="ir.actions.act_window">
        <field name="name">Support Tags</field>
        <field name="res_model">support.tag</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first support tag!
            </p>
        </field>
    </record>
</odoo>
